<h1><%= params[:dice] %>d<%= params[:sides] %></h1>

<a href="/process_roll?dice=<%= params[:dice] %>&sides=<%= params[:sides] %>">Roll again</a> |
<a href="/">Go back home</a>

<ol>
  <% @rolls.each_with_index do |roll, index| %>
    <li><em>Result:</em> <strong><%= roll %></strong></li>
  <% end %>
</ol>
